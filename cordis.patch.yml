# dsh-model-reasoning — bundle patch layer
#
# Declares the plugin as a Cordis loader entry, making the package a standard
# profile bundle: `dsh plugin --profile <p> add dsh-model-reasoning`
# installs and mounts it without manual cordis.patch.yml edits.
- insert:
    - id: reasoning-efforts
      name: 'dsh-model-reasoning'
